diff --git .bazelrc .bazelrc
index d6d9c4c1..7716700e 100644
--- .bazelrc
+++ .bazelrc
@@ -20,7 +20,7 @@ build:remote --remote_timeout=7200
 # ========================================
 
 # Enable libc++ and C++20 by default.
-build:linux --config=clang
+#build:linux --config=clang
 
 # put /usr/local/bin before /usr/bin to avoid picking up wrong python3.6 when building envoy.tls.key_providers.cryptomb
 build:linux --action_env=PATH=/usr/local/bin:/bin:/usr/bin
@@ -52,13 +52,17 @@ build:release --strip=always
 build:release-symbol -c opt
 
 # Debug builds
-build:debug -c dbg
+#build:debug -c dbg
 
 # Add compile option for all C++ files
 # TODO(kuat) re-enable after fixing upstream dd/tracing-lib.
 # build --cxxopt -Wnon-virtual-dtor
 build --cxxopt -Wformat
 build --cxxopt -Wformat-security
+build --cxxopt -Wno-range-loop-analysis
+build --cxxopt --no-warnings
+build --cxxopt -Wno-missing-field-initializers
+build --cxxopt -w
 
 # CI sanitizer configuration
 #
