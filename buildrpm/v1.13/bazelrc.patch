diff --git .bazelrc .bazelrc
index 50ef231..08aa767 100644
--- .bazelrc
+++ .bazelrc
@@ -44,6 +44,9 @@ build:debug -c dbg
 build --cxxopt -Wnon-virtual-dtor
 build --cxxopt -Wformat
 build --cxxopt -Wformat-security
+build --cxxopt --no-warnings
+build --cxxopt -Wno-missing-field-initializers
+build --cxxopt -w

 # Link pthread for flatbuffers
 build --host_linkopt=-pthread
